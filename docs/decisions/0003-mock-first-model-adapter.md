# ADR-0003：先用可解释占位模型跑通系统

- 状态：Accepted
- 日期：2026-07-10

## 背景

同时调试真实模型、容器、网络、调度和指标会使问题难以定位，并可能让模型部署阻塞整个项目。

## 决策

MVP 使用确定性的规则模型；所有模型通过统一 InferenceResult 接口接入。完成主链路后依次替换为 XGBoost、ONNX 和可选量化小模型。

## 后果

- 可以快速验证工程闭环；
- MVP 指标不能用于宣称真实模型性能；
- 真实模型阶段必须关闭测试 confidence 覆盖。
