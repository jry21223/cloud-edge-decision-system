# 贡献规范

1. 从 `main` 创建短生命周期分支：`feat/*`、`fix/*`、`docs/*`。
2. 每个任务必须写清输入、输出、依赖和验收标准。
3. 调度逻辑、接口或架构变化必须同步更新 `docs/`，重要决定新增 ADR。
4. 提交前执行：

```bash
make test
make lint
```

5. 禁止提交 API Key、真实生产数据、大模型权重和本地数据库。
6. PR 应小而可审查；模型、调度器、基础设施变更尽量分开。
